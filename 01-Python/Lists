# List 
List is a collection of items in a particular order.
We can store different type elements inside the list.
Basically list is haterogenious in nature.


Ex: 
name = ['muzaffar', 22, 'Tajpura', 4.5]

# Acessing the list elements

We can acess the list element by providing the index of that element.
Indexing from zero in the list.

Ex: 
name[0] -> muzaffar 
name[-1] -> 4.5 -> -1 is always returns the last value from the list

# modifying elements inside the list

name[0] = 'hussain'

so muzaffar is replace by hussain.

# Modifying the lsit using different methods


guest_list.insert(0,'azeem')  -> adding new element at the of the lsit

guest_list.insert(2,'shadan') -> adding new element in between of the list

guest_list.append('huzaifa')  -> adding at last of the list

# removing 

del  -> del guest_list[0]
pop  -> guest_list.pop(0)


# Sorting a List Permanently with the sort() Method

cars = ['bmw', 'audi', 'toyota', 'subaru']
cars.sort
cars.sort(reverse=True) -> Sorting in reverse order 
print(cars)  

o/p:
['audi', 'bmw', 'subaru', 'toyota']


# Sorting a List Temporarily with the sorted() Function

cars.sorted()   


# Printing a List in Reverse Order

cars.reverse()  -> it just simply reverses the order


# Finding the length of the lsit

print(len(cars))


# loops in list 

loops are basically makes the task easy and fast 
names = ['muzaffar', 'anshul', 'naved']
for in name names:
 print(name)


# range() function

range() function makes it easy to generate a series of numbers.
for value in range(1,5):
print(value)   

O/P ->  1, 2, 3, 4

# Using range() to Make a List of Numbers
number = list(range(1,6))
print(number)

result will be:
[1,2,3,4,5]

# Generating a list of even number 
even_number = list(range(2,11,2))
print(even_number)


# createing a list of containg the sqaure of each number providing in the range
for value in range(1,11):
	squares.append(value**2)

The following example builds the same list of square numbers
this is called list Comprehensions -> doing the same task in less code
	squares = [value**2 for value in range(1,11)]

# simple stats with list of numbers 

 digits = [1, 2, 3, 4, 5, 6, 7, 8, 9, 0]
 min(digits)
0
 max(digits)
9
sum(digits)
45


# Slicing a lsit 

we can work with specific group of items with in a list, which is known as slice in python.


name = ['muzaffar', 'anshul', 'naved', 'vinit']
print(name[0:3])

O/P -> muzaffar, anshul, naved 
python does not include the last number means here we provide(0:3) it only print the elements
of indecies 0,1,2 not 3 .


print(players[:4])

python will autometically stats from first index and print till 3 index 

print(players[2:])

Python returns all items from the third item through the end of the list:


# copying a list 

my_foods = ['pizza', 'falafel', 'carrot cake']

friend_foods = my_foods[:]

[:] -> this will return the all elements of the list or in other words we can say 
we can create the exact copy of the existing lsit.

here we have created the copy of list my_food and stored it in new variable friends_food 



# Tuple
an immutable list is called a tuple.
we can not assign a new value to the item in a lsit.
So if we wanted to change our dimensions, we could
redefine the entire tuple:
dimensions = (200, 50)
print("Original dimensions:")
for dimension in dimensions:
print(dimension)

dimensions = (400, 100)
print("\nModified dimensions:")
for dimension in dimensions:
print(dimension)
